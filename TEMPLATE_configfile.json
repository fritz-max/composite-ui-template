{
    "port": {{ components.composite_ui.port }},
    "remoteModules": {
        {% for mfe in components.microfrontends.values() %}
        "{{ mfe.name }}" : "{{mfe.name}}@{{mfe.address}}:{{mfe.port}}/remoteEntry.js"{{ ", " if not loop.last }}
        {% endfor %}
    }
}
